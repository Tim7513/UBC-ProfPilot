#!/usr/bin/env bash
apt-get update && apt-get install -y \
    libgtk-4.so.1 \
    libgraphene-1.0.so.0 \
    libgstgl-1.0.so.0 \
    libgstcodecparsers-1.0.so.0 \
    libenchant-2.so.2 \
    libsecret-1.so.0 \
    libmanette-0.2.so.0 \
    libGLESv2.so.2